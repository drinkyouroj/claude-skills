# macOS
.DS_Store
.DS_Store?
._*

# Archives (unzip locally, don't commit binaries)
*.zip
*.tar.gz

# Claude Code local machine config — not shared
.claude/

# Workspace scratch dirs (debug artifacts, eval runs)
*-workspace/

# Generated CLAUDE.md files inside skill subdirectories
CLAUDE.md
!/CLAUDE.md

# Eval/review artifacts
*.html

# Node (if any tooling gets added)
node_modules/
